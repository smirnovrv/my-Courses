
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Комментарий = Параметры.Комментарий;
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	 ТекущийКомментарий  =ЗаписатьВХранилище();
	 Оповестить("ТекущийКомментарий",ТекущийКомментарий,);
КонецПроцедуры

&НаСервере
Функция ЗаписатьВХранилище()
    Возврат ПоместитьВоВременноеХранилище(Комментарий); 
КонецФункции